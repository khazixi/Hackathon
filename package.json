{
  "scripts": {
    "dev": "bun run --hot src/index.tsx",
    "css": "bun run tailwindcss -i src/input.css -o src/static/styles.css --watch",
    "debug": "bun --inspect src/index.tsx",
    "generate": "bun drizzle-kit generate:sqlite"
  },
  "dependencies": {
    "@libsql/client": "^0.3.6",
    "@lucia-auth/adapter-sqlite": "^2.0.1",
    "@lucia-auth/oauth": "^3.5.0",
    "drizzle-kit": "^0.20.1",
    "drizzle-orm": "^0.29.0",
    "hono": "^3.9.2",
    "lucia": "^2.7.4"
  },
  "devDependencies": {
    "bun-types": "^1.0.7",
    "tailwindcss": "^3.3.5"
  }
}
